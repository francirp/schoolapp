<h1>Listing rewards</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Points to redeem</th>
      <th>Adult</th>
      <th>Kid</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @rewards.each do |reward| %>
      <tr>
        <td><%= reward.name %></td>
        <td><%= reward.points_to_redeem %></td>
        <td><%= reward.adult_id %></td>
        <td><%= reward.kid_id %></td>
        <td><%= link_to 'Show', reward %></td>
        <td><%= link_to 'Edit', edit_reward_path(reward) %></td>
        <td><%= link_to 'Destroy', reward, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Reward', new_reward_path %>
